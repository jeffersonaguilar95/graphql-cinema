overwrite: true
schema: "./src/**/*.graphql"
documents: null
generates:
  src/common/generatedTypes.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
    config:
      allowParentTypeOverride: false
